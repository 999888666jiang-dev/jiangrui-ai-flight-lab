# 分支计划 - 测试验收

## 目标

建立可重复执行的测试标准。每次页面内容、资源、视觉或交互变化后，至少执行核心回归用例，发现真实问题及时修复。

## 测试环境

- 本地地址：`http://127.0.0.1:5173/`
- 构建命令：`npm run build`
- 浏览器：Chromium via 本机 Chrome
- 自动化：Playwright Core
- Browser 插件：当前会话不可用，因此使用 Playwright 兜底。

## 测试分类

1. 构建测试
   - TypeScript 编译。
   - Vite 打包。
   - Chunk 体积警告记录但不作为失败，当前原生 Three.js 分包较大属于预期。

2. 页面身份测试
   - 标题为 `姜睿 | AI Flight Lab`。
   - H1 为 `姜睿`。
   - SEO 描述无乱码。

3. 内容测试
   - 中文无旧乱码。
   - AI 设计工程师、FPV、全机型无人机、程序员身份线索存在。
   - 证据库 6 个坑位存在。

4. 功能测试
   - 导航锚点可滚动到任务、系统、航迹、联系。
   - `/#/video-bay` 可直达。
   - `/#/evidence-vault` 可直达。
   - 六个展示页可直达：`/#/evidence-vault/fpv-flight-video`、`/#/evidence-vault/ai-design-works`、`/#/evidence-vault/uav-platform-gallery`、`/#/evidence-vault/deal-results-showcase`、`/#/evidence-vault/flight-logs-test-reports`、`/#/evidence-vault/certificates-awards`。
   - 媒体集合页可直达：`/#/evidence-vault/fpv-flight-video/reel`、`/#/evidence-vault/deal-results-showcase/reel`。
   - `/#/evidence-vault/code-projects` 应重定向到 `/#/evidence-vault/deal-results-showcase`。
   - 简历 PDF 可访问。
   - 简历下载入口存在。
   - 语言切换中文和英文有效。

5. 资源测试
   - 首屏图片应加载。
   - 懒加载图片需要滚动到对应章节后再判断。
   - 资源请求不能出现 4xx 或 5xx。

6. 交互测试
   - 桌面端 Three.js 画布存在。
   - 两侧滚动粒子 canvas 存在，滚轮触发后不遮挡正文和按钮。
   - 中区精密粒子 canvas 存在，鼠标和滚动触发后不遮挡标题、按钮、二维码、执照和证据卡。
   - 能力矩阵点击可切换读数。
   - Mission Control 点击可切换任务详情。
   - 从 Evidence Vault 依次点击 6 张资源卡，必须出现吸入式进入动效并进入正确详情页。
   - 详情页返回证据库按钮可用，返回后证据库页面可继续滚动和点击。
   - FPV 和成交成果主舞台视频默认静音自动播放，声音开关只影响当前播放器。
   - FPV 和成交成果随机切换按钮可切换到同组其他视频。
   - 两个查看更多页点击视频卡片可打开灯箱播放器，关闭后保留当前集合页。
   - 鼠标移动不产生运行错误。
   - 章节滚动不产生运行错误。

7. 响应式测试
   - 390x844 移动端无横向溢出。
   - 移动端顶部导航为可横向滑动胶囊。
   - 移动端可直达 Video Bay 和 Evidence Vault。
   - 移动端可直达六个展示页，长中文标题不裁切，不产生横向溢出。
   - 移动端可直达两个媒体集合页，灯箱播放器不横向溢出。
   - 移动端 CTA 可见。

8. 降级测试
   - `prefers-reduced-motion` 时不启用 WebGL，内容仍完整。
   - 微信 UA 模拟时不启用 WebGL，并显示降级提示。

## 本轮测试摘要

日期：2026-06-25

### React 版本首轮测试

- 总用例：29
- 初始结果：28 通过，1 失败
- 失败项：`TC-013 关键图片资源加载成功`
- 复核结论：不是页面问题，是测试时机问题。执照图使用 `loading="lazy"`，首屏不加载；滚动到证书区后加载成功，资源 HEAD 返回 200。
- 修正后结论：按“首屏图片立即检查、懒加载图片滚到章节后检查”的规则，当前页面通过。

### Vue3 迁移回归测试

- 构建命令：`npm run build`
- 构建结果：通过。
- 浏览器：本机 Chrome + Playwright Core。
- 覆盖截图：10 张，保存于 `测试截图/vue3-migration-2026-06-25/`。
- 覆盖范围：首页 Hero、能力矩阵点击、Mission Control 点击、语言切换、联系区、Video Bay、Evidence Vault、移动端首页、移动端 Video Bay、移动端 Evidence Vault。
- 回归结果：通过。
- console warning：0
- page error：0
- 400+ failed response：0

### 两侧滚动粒子回归测试

- 构建命令：`npm run build`
- 构建结果：通过。
- 浏览器：本机 Chrome + Playwright Core。
- 覆盖截图：9 张，保存于 `测试截图/side-scroll-particles-2026-06-25/`。
- 覆盖范围：首页首屏、滚轮触发状态、能力矩阵、Mission Control、Video Bay、Evidence Vault、移动端首页、移动端 Evidence Vault、reduced-motion。
- 回归结果：通过。
- console warning：0
- page error：0
- 400+ failed response：0

### 中区精密粒子回归测试

- 构建命令：`npm run build`
- 构建结果：通过。
- 浏览器：本机 Chrome + Playwright Core。
- 覆盖截图：13 张，保存于 `测试截图/core-motion-particles-2026-06-25/`。
- 覆盖范围：首页首屏、鼠标响应、任务区、能力区、航迹区、Video Bay、Evidence Vault、二维码 hover、移动端首页、移动端 Video Bay、移动端 Evidence Vault、reduced-motion。
- 回归结果：通过。
- console warning：0
- page error：0
- 400+ failed response：0
- 额外修复：Evidence Vault 二维码 hover 扫描线取消线性扫光，防止视觉层压住二维码。

### 六大展示网页回归测试

- 构建命令：`npm run build`
- 构建结果：通过。
- 浏览器：本机 Chrome + Playwright Core。
- Browser 插件状态：当前会话不可用，已记录并使用 Playwright Core 兜底。
- 覆盖截图：20 张，保存于 `测试截图/showcase-pages-2026-06-25/`。
- QA 数据：`测试截图/showcase-pages-2026-06-25/qa-result.json`。
- 覆盖范围：证据库六卡入口、吸入动效中间态、六个详情页桌面首屏、FPV/AI 中段滚动、返回证据库、六个移动端详情页、reduced-motion 直达。
- 路由测试：六个 `/#/evidence-vault/:slug` 均可直达，详情页标题与入口卡片一致。
- 交互测试：从 Evidence Vault 依次点击六张卡均触发 `VaultPortalOverlay`，URL 正确，详情页可返回证据库。
- 移动端测试：390x844 下六个详情页均无横向溢出。
- 降级测试：`prefers-reduced-motion` 下点击卡片直接进入详情页，不播放强吸入动效。
- console warning：0
- page error：0
- 400+ failed response：0
- 额外修复：移动端长中文标题裁切问题已修复，展示页 H1 在窄屏下可正常换行。

### FPV 与成交成果媒体展示回归测试

- 构建命令：`npm run build`
- 构建结果：通过；`three` chunk 体积提示继续记录为非阻断。
- 浏览器：本机 Chrome + Playwright Core。
- 截图目录：`测试截图/media-showcase-upgrade-2026-06-26/`。
- QA 数据：`测试截图/media-showcase-upgrade-2026-06-26/qa-result.json`。
- 覆盖截图：10 张。
- 覆盖范围：FPV 主舞台、FPV 胶卷集合页、FPV 灯箱播放态、成交成果主舞台、成交成果流光展柜、成交成果灯箱播放态、移动端两个详情页、移动端两个集合页。
- 必测交互：声音开关、随机切换、查看更多跳转、视频卡片打开灯箱、关闭灯箱、旧 `code-projects` 路由重定向。
- 网络结果：29 个媒体公开路径 HEAD 均返回 200；中文原始文件名未作为请求路径。
- 移动端结果：390x844 下两个详情页、两个集合页和灯箱播放器均无横向溢出。
- console warning/error：0
- page error：0
- 400+ failed response：0
- 额外修复：移动端灯箱初版被顶部导航压层，已用 Teleport 到 body 修复。

### Video Bay 背景视频纠偏测试

- 背景：用户确认 `展示.mp4` 应放在 `/#/video-bay`，不是 FPV 详情页默认主舞台。
- 构建命令：`npm run build`
- 构建结果：通过。
- 浏览器：本机 Chrome + Playwright Core。
- 截图目录：`测试截图/video-bay-correction-2026-06-26/`。
- QA 数据：`测试截图/video-bay-correction-2026-06-26/qa-result.json`。
- 结果：`public/videos/fpv-lab-background.mp4` 返回 200 且 content-type 为 `video/mp4`。
- 结果：Video Bay 使用 `/videos/fpv-lab-background.mp4`，不再显示 `VIDEO SOURCE PENDING` 占位。
- 结果：Video Bay 默认静音自动播放，声音开关可切换。
- 结果：FPV 详情页使用 `FPV飞行视频/` 文件夹素材，未使用 `展示.mp4`。
- 结果：FPV 胶卷集合页为 11 张卡，不显示 `展示.mp4`。
- console warning/error：0
- page error：0
- 400+ failed response：0

## 已知非阻断警告

- Vite 构建提示 `three` chunk 超过 500KB。
- 来源：Three.js 完整 3D 场景分包。
- 当前影响：不影响页面运行，不产生浏览器 console warning。
- 处理策略：记录为 P3；若后续首屏性能压力增加，再考虑动态导入 Three.js 场景。

## 回归测试触发条件

- 修改 Three.js 场景。
- 修改 `CoreMotionParticles` 或 `SideScrollParticles`。
- 修改 `VaultPortalOverlay`、`ShowcaseDetailPage` 或 `evidenceItems`。
- 修改 `ShowcaseMediaStage`、`ShowcaseReelPage` 或 `showcaseMediaItems`。
- 修改 CSS 布局。
- 新增或替换图片、视频、PDF。
- 修改导航、语言、下载入口。
- 修改移动端或微信降级逻辑。
- 部署前。

## 2026-06-27 全系统适配 P0-P1 测试补充

- 新增适配 QA 维度：`runtimeShell`、`deviceTier`、`prefers-reduced-motion`、触摸设备、iOS/微信视频策略、安全区遮挡、横向溢出。
- Playwright 自动化必须覆盖：`/#/`、`/#/video-bay`、`/#/evidence-vault`、6 个展示详情页、2 个 reel 页、旧 `code-projects` 重定向。
- 视觉截图至少覆盖：桌面首页、视频舱、证据库、FPV 详情、成交成果详情、两个 reel 页、移动端首页、移动端视频舱、移动端证据库。
- 所有视频检查默认静音、`playsinline`、弱网/微信/iOS 播放失败遮罩、声音开关只影响当前播放器。
- 真机仍需单独验收：iPhone Safari、iPhone 微信、Android Chrome、Android 微信、iPad Safari。

## 2026-06-27 P2 与 GitHub 发布准备更新

- 已完成 P2 推荐增强层：PWA manifest、离线壳、Service Worker、安装/更新提示、GitHub Pages 相对路径适配。
- 媒体策略升级为“开发启用本地大视频，生产默认不请求未发布大视频”，避免 GitHub Pages 404 和 GitHub 单文件限制。
- 已准备 GitHub Pages Actions、README、`.env.example`、`.gitignore`、`.gitattributes`、`scripts/sync-github.ps1`。
- QA 通过，记录见 `计划设计/全系统适配P2与GitHub发布准备阶段记录-2026-06-27.md`。
## 2026-06-27 GitHub Releases 视频发布测试补充

- 构建测试：普通构建和带 `VITE_GITHUB_OWNER=test-owner` 的生产 URL 构建均通过。
- Release URL 测试：Playwright 拦截 `/releases/download/`，验证 Video Bay、FPV 详情、成交成果详情、FPV reel 灯箱的 `video.src/currentSrc` 均指向 GitHub Releases URL。
- PWA 缓存测试：Service Worker 注册成功，CacheStorage 未缓存 release/media/videos 视频资源。
- 仓库测试：`git ls-files` 中没有 `.mp4/.mov/.webm/.m4v`，也没有 `个人信息资源包/`、`source-videos/`、`source-documents/`。

## 2026-06-27 手机端/微信视频加载优化测试补充

- 构建测试：`npm run build` 必须通过；`three` chunk 体积提示继续记录为非阻断。
- 脚本测试：未安装 FFmpeg 时，`scripts/prepare-media-variants.ps1` 必须明确失败并提示安装，不得生成错误变体。
- 仓库测试：`git ls-files` 中不得出现原始视频、preview 视频、full 视频、poster 变体。
- 桌面测试：Video Bay、FPV 主舞台、成交成果主舞台优先使用 preview；preview 缺失且开发环境允许回退 full。
- 手机测试：390x844 下 Video Bay、FPV 主舞台、成交成果主舞台不得自动请求 full。
- 微信 UA 测试：默认显示 poster/preview 策略；preview 缺失时显示高级占位，不自动拉取 100MB+ 原始视频。
- Reel 测试：FPV 与成交成果集合页首屏只允许请求 poster，不允许请求 MP4；点击卡片后才允许加载 preview。
- 高清切换测试：点击“查看高清完整版本”后才允许请求 full，且按钮只影响当前播放器。
- 资源释放测试：关闭灯箱、离开路由、低端移动设备下 video src 应释放或暂停，避免后台持续下载。
- CDN 测试：抽查 CDN poster/preview/full URL 返回 200；视频 Range 请求返回 206 或等效支持分段。
- 浏览器质量：console error = 0，page error = 0，400+ failed response = 0，横向溢出 = 0。
