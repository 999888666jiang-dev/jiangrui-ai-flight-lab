# 字体资料库命名与全站稳态审计阶段记录

日期：2026-06-28

## 本次目标

- 修正三处过于拥挤的大标题，让中文标题更大气、留白更稳定。
- 将公开 UI 中的“证据库 / Evidence Vault”统一改为“资料库 / Resource Library”。
- 对当前项目做一次稳态审计：构建、路由、移动端、控制台、横向溢出、旧命名残留、调试代码、明显 AI 味占位和大文件风险。

## 已完成改动

- 首页能力标题改为：`四条证据链，把想法落成作品`。
- 首页经历标题改为：`从前端作品，走向真实场景`。
- 资料库首页标题改为：`资料库：作品、证书与真实能力档案`。
- 导航、Video Bay 入口按钮、详情页返回按钮、404 空态和 MissionRail 均改为资料库语义。
- 保留原 `/#/evidence-vault` 路由，新增 `/#/resource-library`、`/#/resource-library/:slug`、`/#/resource-library/:slug/reel` 兼容别名并重定向到现有资料库路由。
- 新增局部标题排版类，专门作用于能力标题、经历标题和资料库标题；未改全站标题基础规则。
- 媒体空态大写 `SOURCE PENDING` 改为 `SOURCE STANDBY`，降低临时模板感。
- Service Worker 注册失败不再输出 `console.warn`，改为在 `<html>` 上标记 `data-service-worker="unavailable"`，避免用户控制台噪音。

## 审计结论

- `npm run build` 通过。
- `git diff --check` 通过。
- 静态扫描结果：
  - `console.log/debug/warn/error`：0。
  - `TODO/FIXME/HACK`：0。
  - `@ts-ignore/@ts-expect-error`：0。
  - 显式 `any`：0。
  - 可见旧命名 `证据库 / Evidence Vault / EVIDENCE VAULT`：0。
  - `SOURCE PENDING`：0。
- `git ls-files` 未发现 `.mp4/.mov/.webm/.m4v` 被 Git 跟踪。
- 当前最大文件仍为 `src/styles/index.css`，约 110KB；属于后续可拆分维护风险，但本轮不做激进拆分，避免影响选择器优先级和视觉效果。
- `src/data/mediaVariantsManifest.ts`、`src/data/uavGalleryManifest.ts` 是生成型 manifest，体积较大但有明确用途，不属于垃圾代码。
- `HeroLanyardCard.vue`、粒子层、Three.js 场景均为当前可见核心动效，不做删除或重写。

## 浏览器 QA

截图与 QA JSON 保存于：

- `测试截图/typography-library-audit-2026-06-28/before/`
- `测试截图/typography-library-audit-2026-06-28/after/`

覆盖视口：

- 桌面：1366x900。
- 平板：834x1194。
- 手机：390x844、320x568。

覆盖路由：

- `/#/`
- `/#/video-bay`
- `/#/evidence-vault`
- `/#/resource-library`
- `/#/evidence-vault/fpv-flight-video`
- `/#/evidence-vault/deal-results-showcase`
- `/#/evidence-vault/fpv-flight-video/reel`
- `/#/evidence-vault/certificates-awards`
- `/#/evidence-vault/uav-platform-gallery`

验收结果：

- console error/warning：0。
- page error：0。
- 400+ failed response：0。
- 横向溢出：0。
- 旧“证据库 / Evidence Vault / SOURCE PENDING”可见残留：0。

## 保守处理说明

- 本轮没有删除资源占位型 `待补充 / pending` 内容，因为它们用于真实素材未补齐时的稳定空态，不属于无用代码。
- 本轮没有拆分 `index.css`，仅记录为后续维护优化项；若拆分，必须保留 import 顺序并逐页截图回归。
- 本轮没有改媒体 manifest、CDN/COS/GitHub Releases 策略、视频播放策略、粒子系统、挂绳卡片和右下大桨叶逻辑。

## 后续建议

- 下一轮如果继续做“去 AI 味 / 代码瘦身”，建议先做 CSS 覆盖率报告，再按 `hero / navigation / showcase / media / gallery / certificate / effects` 分区拆样式。
- 对大型生成 manifest 保持脚本化更新，不手写清理。
- 对动画组件只做性能门控和命名整理，不做没有截图证据的大范围重构。
