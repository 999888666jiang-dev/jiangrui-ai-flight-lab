# GitHub Pages production builds should leave local media disabled unless large
# videos are uploaded outside the repository or intentionally tracked elsewhere.
VITE_ENABLE_LOCAL_MEDIA=false

# Optional production video source for Video Bay.
# Supports relative public paths or absolute CDN/HLS URLs.
VITE_VIDEO_BAY_SRC=
